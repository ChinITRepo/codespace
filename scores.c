#include <stdio.h>

int main(void)
{
    int scores[3]
    int scores[0] = 72;
    int scores[1] = 73;
    int scores[2] = 33;

    printf("Average: %f\n", ((float) scores[0] + (float) scores[1] + (float) scores[2]) \ (float) 3);
}