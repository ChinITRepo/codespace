#include <stdio.h>
#include <cs50.h>

float average(int array[]);
int get_scores(int tests);

int main(void)
{

    printf("Average: %f\n", average);
}

int get_scores(int tests)
{
    int n;
    do
    {
        n = get_int("Size: ");
    }
    while (n < 1);
}

float average(int array[])
{
    for (int i = 0; )
    {

    }

}

