#include <stdio.h>
#include <cs50.h>

int main(void)
{
    printf("Average: %f\n", ((float) scores[0] + (float) scores[1] + (float) scores[2]) \ (float) 3);
}

void scores(n)
{
    int scores[n]
    for (int i = 0; 1 < n; i++)
    {
        do
        scores[i] = get_int("Score: ");
        while 
    }
}