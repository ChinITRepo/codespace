#include <stdio.h>
#include <cs50.h>

int scores();
float average();

int main(void)
{
    get_int[]
    scores(3);
    printf("Average: %f\n", average);
}



int scores(n)
{
    int scores[n]
    for (int i = 0; 1 < n; i++)
    {
        do
        scores[i] = get_int("Score: ");
        while (scores[i] > 1);
    }
}