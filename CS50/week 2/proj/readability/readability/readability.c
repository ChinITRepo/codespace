#include <cs50.h>
#include <stdio.h>
#include <string.h>

int count_letters(string text);

int main(void)
{
    string text = get_string("Test: ");
    count_letters(text);
}

int count_letters(string text)
{
    int length = strlen(text);

    for (i = 0; i < length; i++)
    {
        if (text[i] >=)
        {

        }
    }
}
