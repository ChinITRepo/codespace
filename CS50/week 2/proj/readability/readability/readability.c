#include <cs50.h>
#include <stdio.h>

int main(void)
{

}

int count_letters(char *s)
{
    int length = strlen(s);

    for (i = 0; i < length; i++)
    {
        if (s = )
        {

        }
    }
}
